# امنیت پورتال کستینگ — فقط همین پوشه (سایت اصلی وردپرس را تغییر نمی‌دهد)

Options -Indexes

# جلوگیری از کش شدن فرم‌های ثبت‌نام / ورود
<IfModule mod_headers.c>
  Header set Cache-Control "no-store, no-cache, must-revalidate, max-age=0"
  Header set Pragma "no-cache"
  Header set X-Content-Type-Options "nosniff"
  Header always set X-Frame-Options "SAMEORIGIN"
  Header always set Referrer-Policy "strict-origin-when-cross-origin"
  Header always set Permissions-Policy "geolocation=(), microphone=(), camera=()"
</IfModule>

<IfModule LiteSpeed>
  CacheDisable public /
  CacheDisable private /
</IfModule>

# مسدود کردن فایل‌های حساس در ریشه پورتال
<FilesMatch "(?i)^(config(\.local)?\.php|\.env|composer\.(json|lock)|.*\.(sql|bak|log|ini))$">
  <IfModule mod_authz_core.c>
    Require all denied
  </IfModule>
  <IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
  </IfModule>
</FilesMatch>

# جلوگیری از اجرای مستقیم فایل‌های includes و mu-plugin از وب
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteRule ^(includes|mu-plugin)/ - [F,L]
</IfModule>
