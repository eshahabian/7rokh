<?php
/**
 * کپی کنید به config.local.php و مقادیر را پر کنید.
 * این فایل در git ذخیره نمی‌شود و با deploy خودکار کپی نمی‌شود.
 *
 * WebOne / Otino:
 * الگو = بخش ثابت + یک متغیر انگلیسی مثل {x}
 * مثال: کد ورود شما {x}
 * کد ۱۹ = متن با الگو یکی نیست
 * کد ۲۳ = داخل متغیر لینک یا IP است
 */
define('CASTING_SMTP_PASS', 'رمز-ایمیل-noreply-اینجا');

define('CASTING_SMS_API_KEY', 'کلید-api-اینجا');
define('CASTING_SMS_FROM', '9998624065');
define('CASTING_SMS_OTP_SENDER', 'Auto');

/** باید عین الگوی تأییدشده در پنل باشد (یک {x}) — برای متن SmartOTP */
define('CASTING_SMS_OTP_TEMPLATE', 'کد ورود شما {x}');

/** کد الگو از: وب سرویس → الگوی پیام وب سرویس (بعد از تأیید اپراتور) */
// define('CASTING_SMS_OTP_PATTERN_ID', '12345');

// define('CASTING_SMS_USERNAME', 'نام-کاربری-پنل');
// define('CASTING_SMS_PASSWORD', 'رمز-پنل');

// define('CASTING_MOBILE_OTP_ENABLED', true);

/**
 * درگاه به‌پرداخت ملت — فقط روی سرور در config.local.php (نه در git)
 * دامنه بازگشت باید 7rokh.ir باشد و IP خروجی هاست در به‌پرداخت ثبت شود.
 */
// define('CASTING_GATEWAY_MODE', 'live');
// define('CASTING_BEHPARDAKHT_TERMINAL_ID', '');
// define('CASTING_BEHPARDAKHT_USERNAME', '');
// define('CASTING_BEHPARDAKHT_PASSWORD', '');
